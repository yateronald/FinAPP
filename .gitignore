# ===========================================================================
# Repository-wide ignores. Per-project rules live in backend/, web/, mobile/.
# ===========================================================================

# --- Secrets -----------------------------------------------------------
# The deploy script carries the production database password and the live
# Gemini / AgentRouter keys. It must never reach GitHub.
db_finace.sh
db_to_aiven.sh
db_to_aiven.sh
backend_finance.sh
*.secrets.env
serviceAccount.json
.env
.env.*
!.env.example

# OAuth client secret bundles downloaded from Google Cloud Console.
client_secret*.json
*.p12
*.pfx
*.jks
*.keystore

# --- Local dumps & backups ---------------------------------------------
*.dump
*.sql.gz
backups/

# --- Logs ---------------------------------------------------------------
*.log
logs/

# --- OS / editor --------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
