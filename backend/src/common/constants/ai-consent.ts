/**
 * Version of the disclosure shown before enabling AI processing.
 *
 * Bump this when the categories of data, purpose, or external AI processors
 * materially change. The backend will then require fresh consent.
 */
export const AI_CONSENT_VERSION = '1.0';

